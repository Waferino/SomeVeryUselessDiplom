CREATE TABLE `www0005_base`.`account` (
  `id_man` INT NOT NULL,
  `Email` VARCHAR(45) NOT NULL,
  `Password` VARCHAR(30) NOT NULL,
  `Role` VARCHAR(10) NOT NULL,
  `date_of_change` VARCHAR(23) NOT NULL,
  PRIMARY KEY (`id_man`));
